import styles from "./styles.module.scss";

function RadioGroup({
  id,
  label,
  data,
  handleCheck,
  handleSearch,
  haveSearch,
  disable,
  loadding,
}) {
  return <h1>RadioGroup</h1>;
}

export default RadioGroup;
