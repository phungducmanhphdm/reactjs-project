import styles from "./styles.module.scss";

function CheckGroup({
  id,
  label,
  data,
  handleCheck,
  handleSearch,
  haveSearch,
  disable,
  loadding,
}) {
  return <h1>CheckGroup</h1>;
}

export default CheckGroup;
